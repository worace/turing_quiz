<%= form_for([@question, @further_thought]) do |f| %>
  <% if @further_thought.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@further_thought.errors.count, "error") %> prohibited this further_thought from being saved:</h2>

      <ul>
      <% @further_thought.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
